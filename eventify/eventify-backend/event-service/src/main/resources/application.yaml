server:
  port: 8082

spring:
  application:
    name: event-service
  datasource:
    url: jdbc:postgresql://localhost:5434/event_db
    username: postgres
    password: postgres

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000